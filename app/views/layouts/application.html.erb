<!DOCTYPE html>
<html lang="en">
  <head>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= yield :jcrop_script %>
    <%= csrf_meta_tags %>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= yield :head %></title>
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body class="<%= yield :body_class %>">
    <% if user_signed_in? %>
      <%= render "layouts/header" %>
      <div class="">
        <div class="" style="display:none; margin-top: 200px;">
          Your profile has been successfully updated. Now, Lets get Started!!
        </div>
      </div>
      <% if session[:modal] %>
        <div class="modal fade show in" id="new-user" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" style="background-color: rgba(39, 39, 37, 0.53);">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel">Welcome to GalleryProofing!</h4>
              </div>
              <%= render partial: 'users/edit_user'%>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  <%= yield %>
</body>
</html>
