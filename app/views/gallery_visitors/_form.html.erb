<%= form_for(@gallery_visitor) do |f| %>
  <% if @gallery_visitor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gallery_visitor.errors.count, "error") %> prohibited this gallery_visitor from being saved:</h2>

      <ul>
      <% @gallery_visitor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :gallery_id %><br>
    <%= f.text_field :gallery_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
