$("Two").css("display","none")

var email='<%= @email_status %>'
var password='<%= @pass_status %>'
var access_code='<%= @code %>'


if(email == "true" && password == "true")
{

  if(access_code== "true")
    {
      // $("#full-cons").modal('hide');
      $("#One").css("display","none");
      $("#Two").css("display","block");
      // $('#myModal').modal({  show: true,  });

    }
    else
    {
      // window.location.href = "http://localhost:3000/<%= params[:brand] %>/gallery/<%= params[:gallery] %>/home";
      // window.location.href = "javascript:document.location.href=getUrl();"
      window.location.replace("<%= view_home_path(params[:brand] , params[:gallery]) %>");
    }
}

else {
  $('#error_for_password').html("The password you entered is incorrect.");
}
//
// function getUrl()
// {
//   var hostname = window.location.origin
//   var fullpath = hostname + "/<%= params[:brand] %>/gallery/<%= params[:gallery] %>/home"
//   return fullpath;
// }
