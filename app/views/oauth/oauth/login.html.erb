


    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->




    <!-- wrapper -->
    <div class="container-fluid">
    	<div class="row">
			<div class="new-login-box">
				<%= form_tag oauth_login_attempt_path do |f| %>

					<div class="form">
						<div class="logo-white"></div>
						<p>It is a long <strong>established fact that a reader.</strong> <br> Ipsum is that it has a more-or-less normal.</p>

						<% flash.each do |key, value| %>
						  	<%= content_tag :div, value, class: "flash #{key}" %>
						<% end %>

						<div class="form-group">
							<%= text_field_tag "email" ,"",:class => "form-control" %>
						</div>
						<div class="form-group">
							<%= password_field_tag "password" , "" ,:class => "form-control" %>
						</div>
						<div class="mew-login-btn">
							<button type="submit" class="btn btn-primary">Log in</button>
						</div>
						<p class="certificate-status">© 2016 GalleryProofing, LLC</p>
					</div>	
				<% end %>
			</div>  	
    	</div>
    </div>
	<!-- /wrapper -->

