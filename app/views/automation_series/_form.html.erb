<%= form_for(@automation_series) do |f| %>
  <% if @automation_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@automation_series.errors.count, "error") %> prohibited this automation_series from being saved:</h2>

      <ul>
      <% @automation_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title" id="myModalLabel">Rename</h4>
  </div>

  <div class="modal-body">
      <div class="form-group">
        <%= f.text_field :name, class: "form-control" %>
      </div>
  </div>
  <div class="modal-footer">
    <%= link_to "Cancel", class: "btn btn-default", "data-dismiss": "modal" %>
    <%= f.submit "Rename", class: "btn btn-primary" %>
  </div>
<% end %>
