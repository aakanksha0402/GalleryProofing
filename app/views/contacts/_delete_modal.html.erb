<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
  <h4 class="modal-title" id="myModalLabel">Delete Contact?</h4>
</div>
<div class="modal-body">
  <form>
    <p>Are you sure that you want to delete Contact "<%= @f_name + " " + @l_name %>" ?</p>

    <p>This will also remove the Contact from any Galleries that they are assigned to.</p>

    <p>This action <strong>cannot be undone!</strong></p>
  </form>
</div>
<div class="modal-footer">
  <%= link_to "Cancel", "#", class: "btn btn-default", "data-dismiss": "modal" %>
  <%= link_to "Yes, Delete", contact_path(id: @c), class: "btn btn-sm btn-danger", method: :delete, data: { disable_with: "Please wait..." } %>
</div>
